# v1.7.1

修复守护生命周期和每日计划任务的可靠性问题。

## 更新内容

- 修复停止后快速重启可能产生多个守护线程的问题；旧线程状态不会覆盖新一轮守护。
- 手动守护持续运行直到用户停止，不再受默认 60 分钟时限影响。
- 每日任务使用 `--silent --now`，由 Windows 触发时间决定何时开始，避免重复等待。
- 任务注册失败不再因旧任务存在而误报成功；注册后核对程序路径、参数、工作目录和触发时间。
- 修复含单引号路径的 PowerShell 转义；校验每日启动时刻和正整数运行时长。
- 发布说明纳入源码管理，发布包继续按白名单排除真实账号配置和日志。

## 下载与升级

下载 `auto_login_v1.7.1.zip` 并解压，运行 `CampusNet.exe`。保留原有 `auto_login_config.json`，不要用示例配置覆盖已有账号密码。

升级前退出正在运行的旧版程序。升级后在「每天自动守护」页重新点击「开启每天自动守护」，以更新已有计划任务的启动参数。

## 验证

- 39 项离线回归测试通过，包含真实 PowerShell 执行环境中的模拟任务注册校验。
- 发布包使用白名单组装，不含真实配置或日志。
- 未进行真实校园网认证测试。
