# v2.0

这版聚焦每天需要重新认证时的自动恢复：补齐每次认证的分段观测、失败分类和有界重试调度。校园网是否接受认证由门户决定；本工具不承诺固定秒数恢复，也不会主动注销现有会话。

## 主要变化

- 记录门户发现、登录 POST、联网复核、总耗时及未归入阶段的耗时；日志在程序启动时记录版本、运行路径与 EXE SHA256，便于确认实际运行副本。
- 对来自门户和网络的日志采用白名单式摘要，避免把密码、完整查询参数、原始响应体或会话令牌写入日志。
- 将实测 `mac collision`、门户“设备未注册”、凭据错误、网络错误和未知错误分开记录。只有明确的 `mac collision` 具备有限快速重试资格。
- **快速重试默认关闭**，旧配置无需新增字段。只有显式配置启用时才按预算和窗口运行；其他失败保持常规退避。
- 断网首次确认后，后续认证仅按重试时刻调度，不再为每次尝试重复累计断网确认阈值。认证成功仍须经过联网复核。

## 验证及边界

- 阶段 A 观测版与阶段 B 候选版曾在本机每日 30 分钟守护窗口内试用。09-24 的正常每日重新认证：前两次门户报告 `mac collision`，第三次成功，从首次失败探测到确认恢复约 12 秒，共 3 次认证。
- 09-23 对 1 秒快速重试做过一次受限试用：3 次快速重试均被拒绝，最终回到常规退避后成功；没有观察到提速收益，因此日常运行配置已恢复为默认关闭。不同日期不能作严格因果对照。
- 09-24 19:36～19:37 另有探测超时及“设备未注册”事件，发生在用户可能手动给同账号手机认证的时段；其成因未证实，不作为每日自动认证的干净样本，也不宣称本版解决了同账号设备抢占。
- 149 项离线测试、`py_compile` 和 `git diff --check` 通过。冻结版归档中直接确认内嵌 `v2.0`，EXE SHA256 为 `BF3E7B4334BF9828164E01D781E172622F02788FE07F716EA1475ECC6FE741AD`。
- 用隔离目录、仅指向本机空端口的假认证配置做 1 分钟静默冒烟：约 61.6 秒退出、退出码 0、全程没有窗口句柄；日志记录 `version=v2.0` 且没有访问真实校园门户。此冒烟不等于真实门户认证测试。09-24 的真实每日认证发生在此前的阶段 B 候选版上；v2.0 除版本标识外未改变该认证逻辑。

## 升级

下载正式的 `auto_login_v2.0.zip` 后，保留自己的 `auto_login_config.json`，不要把含密码的配置或日志上传分享。若只在原目录替换 `CampusNet.exe`，现有每日计划任务的程序路径无需改变；若改了目录，应在新位置重新部署每日守护。快速重试开关缺省为关闭。
