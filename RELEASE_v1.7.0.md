# v1.7.0

校园网断线自动认证工具。定时检测网络状态，检测到 captive portal 后通过后台 HTTP 请求静默完成认证，**完全无感**。

这一版的重点是**做了一层图形界面**，让不需要懂命令行的人也能直接用起来。

## 下载与安装

下载 `auto_login_v1.7.0.zip`，**整个文件夹一起解压**（`assets` 子目录是图标，不能少），然后双击 `CampusNet.exe`。

不需要安装 Python，不需要改任何文件。第一次运行会在同目录自动生成：

```
auto_login_config.json   ← 配置（含密码，程序自己创建）
logs\                    ← 运行日志，按日期命名，自动清理 7 天前
```

在界面「设置」页填好**学号**和**密码**，就能用了。三亚学院的认证地址已经预填好，不用改。

## 两种用法，各占一个选项卡

### 防呆：账号密码没填就开不了

每日任务失败时是**完全静默**的（这正是场景 B 的意义，不弹任何窗口），所以必须在创建任务那一刻就拦住，否则你会得到一个「到点必然失败、却毫无提示」的任务，只能翻日志才发现。现在：

- **没填账号密码时，主按钮和「开启每天自动守护」都是灰的**，直接写着「请先填账号密码再开启」
- 点下去会告诉你还缺哪些项（学号 / 账号、密码），并自动切到「设置」页
- 照抄示例配置没改（`你的学号` / `你的密码`）或用空格填充，同样算作未填
- 判定按认证方式区分：`portal_post` 要学号+密码，`http` / `browser` 要校园网认证地址

### 「每天自动守护」（默认页，多数人用这个）

1. 切到这一页
2. 填「每天 23:00 启动，守护 30 分钟后自动停止」
3. 点 **「开启每天自动守护」**，在弹出的 UAC 窗口点「是」

之后每天到点系统会自己拉起它，**全程无窗口、无托盘图标、不弹任何东西**——你甚至不知道它在跑。它持续探测网络，断网那一刻立即自动重连，跑满时长后自己退出，当天只在 `logs\` 里留一份日志。

想先确认效果，点同一页的「测试一次静默运行」：它会悄悄跑 1 分钟，你会看到真的什么都不出现。

### 「手动守护」（临时用）

点 **「开始守护」**，它就一直盯着网络，断网立刻自动重连。点右上角 **✕** 会缩到右下角托盘继续守护；托盘右键可「打开主窗口」或「退出」。

### 「开启每天自动守护」的权限提示

不是管理员身份打开也没关系——点按钮时会弹系统权限窗口（UAC），点「是」即可，**不需要专门用管理员身份重开程序**。具体行为：

- 点按钮后**先弹一个说明框**，告诉你接下来会出现系统权限窗口，点「是」就行，避免你以为程序卡了或者被弹窗吓到
- 权限窗口点「是」→ 创建成功后弹「设置好了」，并复述时间/时长
- 权限窗口点「否」→ 明确告诉你"你取消了，任务没有创建"，并给两条出路：右键 `CampusNet.exe` →「以管理员身份运行」再点一次，或者复制给出的命令行在管理员 PowerShell 里执行
- 创建结果**以计划任务服务为准**（用 `schtasks /Query` 复核），而不是只看脚本退出码——避免出现"提示成功但其实没建成"

## 这次做了什么

### 新增：图形界面

- 三个选项卡：**手动守护** / **每天自动守护** / **设置**
- 状态用大字号 + 彩色圆点表示：待命中（灰）/ 守护中（绿）/ 正在重连（黄）/ 重连未成功（红）
- 日志默认展开，但**显示的是人话**：「检测到断网，开始自动重连…」「自动重连成功，网络已恢复（断网时长 1m2s）」。底层原始记录收进「详细日志」，排障时才需要
- 托盘图标会跟着状态更新提示文字

### 新增：`--silent` 静默守护

无窗口、无托盘、无控制台，只写日志：

```powershell
CampusNet.exe --silent                              # 等到配置里的时刻再开始
CampusNet.exe --silent --now --run-minutes 1        # 立刻试跑 1 分钟
```

**这个能力只有 `CampusNet.exe` 有。** 它是用 `--windowed`（GUI 子系统）打包的，进程从出生就没有控制台；旧的 `auto_login.exe` 是 `--console` 版本，被计划任务拉起时 Windows 一定会先弹一个黑框再靠代码藏起来——那一瞬间是看得见的。

### 修复：exe 读不到配置（重要）

这是 v1.6.1 那版 exe 的一个隐蔽问题：程序按自身所在目录找 `auto_login_config.json`，如果你把它放在子目录里运行（比如 `dist\`），它会**找不到配置、静默退回默认值**——没有账号、检测间隔变成 5 秒、运行时长变成 60 分钟，同时把日志写进 `dist\logs\`。现象就是你明明改了设置却完全不生效。

现在规则很简单：**配置就在 exe 旁边**，找不到就自动生成一份。

### 修复：窗口显示后自动消失

复用控制台版的托盘代码时，它的「隐藏控制台」逻辑会枚举进程内所有可见窗口一并隐藏，把 Tk 主窗口也藏了。窗口出现约 0.3 秒后消失且没有任何报错，就是这个原因。

### 其他修复

- 退出时刷屏 `TclError`：窗口销毁后 120ms 轮询定时器仍在操作已销毁的控件
- 静默时刻算法：手动启动时若当天窗口已过，会空等 24 小时，现在立即开跑
- `setup_task.ps1` 新增 `-Silent` / `-SilentAt` / `-RunMinutes`

## 命令行等价写法

界面上的按钮只是把下面这些命令包了一层，喜欢命令行的可以直接用：

```powershell
# 每天 22:50 起静默守护 30 分钟（管理员 PowerShell）
.\setup_task.ps1 -Silent -SilentAt 22:50 -RunMinutes 30

# 删除
Unregister-ScheduledTask -TaskName CampusNetAutoLogin_Silent -Confirm:$false

# 图形界面 / 手动守护
python gui_app.py

# 单次认证测试（返回码 0 成功 / 1 失败）
python auto_login.py --auth
```

## 注意事项

- 仅支持 Windows 10/11
- 默认配置为三亚学院校园网，换学校需改「校园网认证地址」
- 工具**不会主动登出**校园网。因此网络正常时测试认证可能因缺少认证参数而报错（如"设备未注册"），这属正常现象，**不代表配置有问题**——最准确的测试方式是在真实断网时
- 使用代理软件（Clash 等 TUN 模式）时，认证请求可能被路由到虚拟网卡，导致 portal 收到错误的客户端 IP，认证期间建议关闭 TUN
- 「开启每天自动守护」需要管理员权限（弹 UAC 是正常的），因为注册计划任务必须有权限

**免费开源，仅供学习研究使用。如付费获取请立即退款举报。**
