# v1.7.5

补齐 v1.7.4 异常恢复里最后一步：托盘失效时，把已经缩起来的窗口找回来。

## 修复

- **窗口缩到托盘后托盘失效，程序会变成"看不见还在跑"。** v1.7.4 让 GUI 在托盘失效时退回无托盘窗口模式，但只清掉了 `_tray` 引用：如果用户先点过 ✕（窗口 `withdraw()`，而且不再有任务栏按钮），而托盘线程随后异常退出，那么**托盘这个唯一入口就没了**，窗口还在，但没有任何办法再把它调出来。

  现在退回无托盘模式时，只要窗口不可见（`_window_hidden()`：`withdrawn` 或 `iconic`）就调用 `_show_window()` 把它显示出来，并在日志面板说明"托盘没了，请从窗口退出"。本来就显示着的窗口不会被动去抢焦点；`root.state()` 读取失败（窗口已销毁等）按"没隐藏"处理，不让恢复逻辑变成新的崩溃点。

## 升级说明

下载 `auto_login_v1.7.5.zip` 解压后运行 `CampusNet.exe`；也提供独立 exe 和 SHA256 校验文件。旧账号配置不会自动覆盖：把原目录里的 `auto_login_config.json` 复制到新目录，或在界面「设置」页重新填一次。更换程序目录后需要重新部署每天自动守护。

v1.7.4 的附件没有改动，仍然可以下载；只有在托盘失效这种异常情况下才需要这一版。

## 本次验证的边界

- 运行级 A/B（源码运行，一次运行里同时看到隐藏与恢复）：把 `GUITray.run` 换成"4 秒后返回 `False`"、t=2s 触发 `_on_close_request` 模拟点 ✕ —— t=3s `MainWindowHandle=0`（确实隐藏），t=8s 句柄恢复（值 `3280496`）且标题为 `校园网自动认证 v1.7.5`，进程全程存活。
- 单元测试 4 项覆盖该分支：隐藏时恢复、最小化（`iconic`）也恢复、本来就可见时不抢焦点、`state()` 抛异常不致命。共 **72 项离线测试通过**。
- 冻结版 exe 的冒烟只覆盖常规路径（启动、`--silent`、启动界面不产生任何探测日志）；"托盘先失效、窗口已隐藏"这条时序无法在冻结产物里注入，因此它由源码运行级复现 + 单元测试保证。
- 仍未实机复验：真实开启受控文件夹访问的桌面（回归测试是打桩"程序目录不可写"）。

## 已复验（2026-09-14，Windows 11，CampusNet v1.7.5）

- **UAC 提权正向路径**：未提权双击运行 → 「开启每天自动守护」→ 说明框 → 系统权限窗口点「是」→ 程序以管理员身份重启 → 再点一次按钮 → 「已创建每日任务 ✓」，任务名 `CampusNetAutoLogin_Silent` 状态 `Ready`、动作与触发时刻与设置一致。
- **UAC 提权反向路径（创建）**：同样流程在权限窗口点「否」→ 弹出专门的「你取消了权限请求」提示（"你在系统权限窗口点了「否」，所以什么都没做"），界面提示「已取消，没有做任何改动。」，程序不关闭、不重启，任务未被改动。
- **UAC 提权反向路径（删除）**：在「每天自动守护」页点「取消每天自动守护」→ 说明框 → 权限窗口点「否」→ 同样的取消提示，且任务仍在（`State` 仍为 `Ready`）—— 证明删除路径也接上了提权门。
- 已知小瑕疵（不影响功能，留待下一版）：取消提示写的是"点了「否」"，而标准用户看到的是"输入管理员密码"的凭据窗口、只有「取消」按钮。文案对管理员账户（是/否同意框）是准确的。

