# v1.7.2

- 包含 v1.7.1 守护线程修复及后续按钮状态一致性修复。
- 首次认证失败后等待 3 秒，随后按 5、10、20、40…秒递增，最长 300 秒；实际请求时间还受检测轮询及网络耗时影响。
- 新增 auth_retry_backoff_seconds 配置，默认 5 秒。
- 41 项离线测试覆盖认证、重试、线程生命周期和任务注册校验。

升级时保留账号配置，并重新部署计划任务使其指向新 exe。

## 升级说明

下载 auto_login_v1.7.2.zip 解压后运行 CampusNet.exe；也提供独立 exe 和 SHA256 校验文件。

旧账号配置不会自动覆盖。要启用新版快速重试，请在实际使用目录的 auto_login_config.json 中设置 `auth_cooldown_seconds: 3` 和 `auth_retry_backoff_seconds: 5`，并重启守护。更换程序目录后需要重新部署每日计划任务。

本次仅做离线回归、exe 启动及已在线状态的静默检测验证，未主动断网验证新版认证时延。
