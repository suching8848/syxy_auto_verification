# v1.7.4

修掉 v1.7.3 里已经公开、但当时没有修的问题，另修一处 UAC 取消判读。

## 主要修复

- **[P1] GUI 不再自带一路守护。** 以前打开界面时，托盘线程会自行启动一个探测循环（日志里表现为启动即出现 `Service started [tray]`），点「开始守护」又起第二路；「停止守护」只停得掉界面那一路，托盘那一路要等它自己跑满 `run_duration_minutes`（默认 60 分钟）或退出程序才结束 —— 也就是说停止后界面显示「待命中」，后台可能仍在探测与重连。

  现在 `TrayApp` 多了一个 `start_worker` 开关，GUI 的托盘以 `start_worker=False` 创建：守护线程只归 `start_monitor()` / `stop_monitor()` 所有，点「停止守护」就是真的停了。

  **异常路径同样堵上了**：`TrayApp.run()` 里"窗口/图标建不出来，回退成控制台探测"那条分支以前不看 `start_worker`，也不传停止事件——托盘初始化失败时照样会凭空起一路探测，而且它连停都停不掉。现在这一路分开了：GUI（`start_worker=False`）初始化失败就返回 `false`、不起任何探测，界面退回**无托盘窗口模式**（窗口继续用，✕ 就是退出，不会缩进一个不存在的托盘）；控制台托盘（`--tray`、菜单 [5]）保留回退探测，并把自己的停止事件传进去，因此可被停止。`run()` 现在返回"托盘是否真的建起来了"，GUI 据此区分「托盘退出 = 关程序」和「托盘压根没起来 = 继续用窗口」。

  **行为变化（也是文档一直描述的行为）**：打开界面后不会自动开始守护，需要点「开始守护」；关窗口缩到托盘会继续守护。控制台托盘模式（`--tray`、菜单 [5]）的守护线程本来就是托盘自己的，行为不变；`--silent` 不创建托盘，走的不是这条路。

- **[P2] UAC 取消能被正确识别了。** `ShellExecuteW("runas", ...)` 的成功与否只能看返回值是否大于 32，失败原因必须紧接着读 `GetLastError()`（取消 UAC 是 1223）。旧代码直接拿返回值跟 1223 比较，取消常常落到通用分支，用户只看到一句没头没尾的"提权启动失败"。现在 shell32 用 `use_last_error=True` 打开、返回值按指针宽度取（HINSTANCE 是指针，默认 `c_int` 会截断），调用与读错误码之间不插任何 Python 代码；取消就是"你在系统权限窗口点了「否」"，其它失败才报完整错误码。

## 升级说明

下载 `auto_login_v1.7.4.zip` 解压后运行 `CampusNet.exe`；也提供独立 exe 和 SHA256 校验文件。旧账号配置不会自动覆盖：把原目录里的 `auto_login_config.json` 复制到新目录，或在界面「设置」页重新填一次。更换程序目录后需要重新部署每天自动守护。

## 本次验证的边界

- P1 的对照证据：修前启动界面，日志立刻出现 `Service started [tray]` + `Scheduled mode`；修后启动界面，日志一行都不新增。单元测试另断言 `start_worker=False` 时不会调用 `run_detection_loop`、默认值仍会调用（控制台模式不能被一起改掉）、`GUITray` 传的是 `False`。
- P1 异常路径：把 `GUITray._create_tray_icon` 强制抛异常后真跑一次界面 —— 进程继续存活且窗口在（`校园网自动认证 v1.7.4`），日志里只有一条 `Tray init failed`，**没有** `Service started`。单元测试另分别覆盖"窗口创建失败"和"图标创建失败"两种情形，以及控制台回退确实拿到了停止事件。
- P2 只有纯函数级测试（`elevation_outcome()`：>32 视为成功、失败时从 `GetLastError()` 读 1223 判为取消、其它失败报两个错误码）。真实 UAC 窗口点「是/否」的交互无法自动化，需要人工点一次确认。
- 受控文件夹访问的回归测试仍是**模拟程序目录不可写**（对 `_dir_writable()` 打桩），不是在真实开启了该功能的桌面上复验。
- 68 项离线测试通过。
