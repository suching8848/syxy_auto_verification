# v1.7.3

- 修复一类「放在桌面的副本建不了每天自动守护，用管理员身份打开也没用」的失败。经代码审查与复现，最可能的成因是 Windows 的**受控文件夹访问**（勒索软件防护）：它拦住未签名程序往桌面 / 文档 / 图片这类受保护目录写文件，而旧版恰好把注册计划任务用的中转脚本写在 exe 旁边——写入被拦之后命令根本没发出去，界面只能报一个没头没尾的失败；提权也救不了，因为拦的是路径不是权限。新版**不再往程序目录写任何中转文件**，这类拦截因此失去了触发条件。
- 注册任务的中转文件改到 `%TEMP%`，并去掉 `.bat` 包装：不再依赖 cmd 的代码页（中文路径在非中文系统上会被解码坏），子进程退出码直接采用，PowerShell 的报错文本按 UTF-8 → 系统 OEM 代码页 → ANSI 依次解码，中文报错不会再变成乱码。
- **提权流程改对了**：非管理员时先征得同意，再以管理员身份重新打开本程序（UAC 由那次启动真正触发），然后才去改计划任务 —— 建任务和删任务都走这条门。旧流程是"就地跑一遍 → 必定被拒 → 再问要不要提权重开"，而界面同时承诺"接下来会弹出系统权限窗口"——在旧流程里那句话是假的：就地跑根本不会弹 UAC。删任务以前更是完全没有这道门，非管理员点「取消每天自动守护」只会得到一句"删除失败"。
- 程序目录写不进去时（受控文件夹访问或只读安装目录），配置与日志整体回退到 `%APPDATA%\CampusNet`，**读取也跟随**那一份；界面日志里会明说设置存到了哪里。否则会出现"设置写进 %APPDATA%、却还在读桌面上的旧文件"的错位，静默守护也会一条日志都不留。
- 计划任务注册/删除失败时，「程序回报」原文用 `core.log()` 写进 `logs\`（此前只是显示在界面文本框里，关掉窗口就没了），远程排查只要对方发日志，不用截图。
- 55 项离线测试通过（新增 14 项：路径回退、命令执行链、失败详情落盘断言、非管理员分支不再直接跑特权命令）。

## 升级说明

下载 `auto_login_v1.7.3.zip` 解压后运行 `CampusNet.exe`；也提供独立 exe 和 SHA256 校验文件。

旧账号配置不会自动覆盖：把原目录里的 `auto_login_config.json` 复制到新目录，或在界面「设置」页重新填一次。

放桌面、OneDrive 同步目录、`Program Files` 现在都能跑了，但仍建议使用 `D:\CampusNet\` 这类**纯英文短路径**——路径短、无空格、不同步、不受保护机制管。更换程序目录后需要重新部署每天自动守护，让计划任务指向新路径。

## 本次验证的边界

- 受控文件夹访问的回归测试是**模拟程序目录不可写**（对 `_dir_writable()` 打桩），不是在真实开启了该功能的桌面上复验；exe 只做了启动与静默路径冒烟。也就是说"路径回退逻辑正确"有自动化测试保证，"真机上不再被拦"仍需实机确认——门槛很低：装好新版后在桌面上建一次任务即可。
- 提权流程的改动同样只有单元测试覆盖（断言非管理员分支不会去跑注册命令，而是先重开自己）。真实 UAC 窗口点「是/否」的交互无法自动化，需要人工点一次确认。

## 已知问题（既有行为，本次未改）

- 打开界面时，托盘线程会自带一路探测（`core.TrayApp.run()` 无条件启动 worker，日志里表现为 `Service started [tray]`）；再点「开始守护」会起第二路。「停止守护」目前只停掉界面那一路，托盘那一路要等它自己跑满 `run_duration_minutes`（默认 60 分钟）或退出程序才结束——所以停止后界面显示「待命中」，后台可能仍在探测与重连。v1.7.2 及更早版本行为相同（已用两个版本的启动日志对照确认），尚未修复。
